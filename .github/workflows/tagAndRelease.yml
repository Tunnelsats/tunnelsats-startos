name: Tag and Release

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  tag-and-release:
    name: Tag and Release
    uses: Start9Labs/start-technologies/.github/workflows/tagAndRelease.yml@fa6e8eb0dfc2b51238107c9694c689708322b3b2 # master
    with:
      REFERENCE_REGISTRY: 'https://community-registry.start9.com'
    secrets:
      DEV_KEY: ${{ secrets.DEV_KEY || secrets.STARTOS_DEVELOPER_KEY }}
      S3_ACCESS_KEY: ${{ secrets.S3_ACCESS_KEY }}
      S3_SECRET_KEY: ${{ secrets.S3_SECRET_KEY }}
